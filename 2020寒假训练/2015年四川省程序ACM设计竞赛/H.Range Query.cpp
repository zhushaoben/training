#include<bits/stdc++.h>
using namespace std;
#define maxn 55
struct Node{
	l,r,x;
}a[maxn],b[maxn];
int n,m1,m2;
void work(){
	for(int i=0;i<m1;i++)scanf("%d%d%d",&a[i].l,&a[i],r,&a[i].x);
	for(int i=0;i<m2;i++)scanf("%d%d%d",&b[i].l,&b[i],r,&b[i].x);
	for(int i=1;i<=n;i++){
		for(int i=0;i<m1;i++)
	}
}
int main(){
	while(~scanf("%d%d%d",&n,&m1,&m2))work();
	return 0;
}
